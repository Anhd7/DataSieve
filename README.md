# DataSieve
DataSieve is a high-performance CLI tool built with Go that recursively scans files and directories for sensitive data such as emails, phone numbers, and Saudi national IDs. It supports parallel scanning, masked reporting, CI/CD integration, and PDPL-focused compliance auditing across .csv, .json, .txt, and .log files.
